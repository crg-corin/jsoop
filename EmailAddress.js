(function (w) {
    "use strict";
    function EmailAddress(addr) {
        var domain,
            local;
        
        this.local = local;
        this.domain = domain;
    }
    EmailAddress.prototype = {
        
    };
    
    w.EmailAddress = EmailAddress;
}(jsoop || window));